<%= form_with(model: bird_breed) do |form| %>
  <% if bird_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bird_breed.errors.count, "error") %> prohibited this bird_breed from being saved:</h2>

      <ul>
        <% bird_breed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
